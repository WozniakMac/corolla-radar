{
  "type": "object",
  "additionalProperties": false,
  "required": [
    "winnerId",
    "verdict",
    "comparisonSummary",
    "confidence",
    "rankings",
    "commonChecks",
    "dealBreakers"
  ],
  "properties": {
    "winnerId": { "type": "string", "minLength": 1 },
    "verdict": { "type": "string", "minLength": 1 },
    "comparisonSummary": { "type": "string", "minLength": 1 },
    "confidence": { "type": "number", "minimum": 0, "maximum": 100 },
    "rankings": {
      "type": "array",
      "minItems": 10,
      "maxItems": 10,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "rank",
          "carId",
          "recommendation",
          "purchaseScore",
          "scoreBreakdown",
          "rationale",
          "visualAssessment",
          "visualRisks",
          "strengths",
          "risks",
          "nextSteps",
          "negotiationTarget",
          "maxRecommendedPrice"
        ],
        "properties": {
          "rank": { "type": "integer", "minimum": 1, "maximum": 10 },
          "carId": { "type": "string", "minLength": 1 },
          "recommendation": {
            "type": "string",
            "enum": ["kup", "shortlista", "sprawdź", "odrzuć"]
          },
          "purchaseScore": {
            "type": "integer",
            "minimum": 0,
            "maximum": 100
          },
          "scoreBreakdown": {
            "type": "object",
            "additionalProperties": false,
            "required": [
              "value",
              "history",
              "equipment",
              "convenience",
              "evidence",
              "riskPenalty"
            ],
            "properties": {
              "value": { "type": "integer", "minimum": 0, "maximum": 30 },
              "history": { "type": "integer", "minimum": 0, "maximum": 25 },
              "equipment": { "type": "integer", "minimum": 0, "maximum": 15 },
              "convenience": {
                "type": "integer",
                "minimum": 0,
                "maximum": 10
              },
              "evidence": { "type": "integer", "minimum": 0, "maximum": 20 },
              "riskPenalty": {
                "type": "integer",
                "minimum": 0,
                "maximum": 40
              }
            }
          },
          "rationale": { "type": "string", "minLength": 1 },
          "visualAssessment": { "type": "string", "minLength": 1 },
          "visualRisks": {
            "type": "array",
            "items": { "type": "string" }
          },
          "strengths": {
            "type": "array",
            "items": { "type": "string" }
          },
          "risks": {
            "type": "array",
            "items": { "type": "string" }
          },
          "nextSteps": {
            "type": "array",
            "items": { "type": "string" }
          },
          "negotiationTarget": {
            "type": ["number", "null"],
            "minimum": 0
          },
          "maxRecommendedPrice": {
            "type": ["number", "null"],
            "minimum": 0
          }
        }
      }
    },
    "commonChecks": {
      "type": "array",
      "items": { "type": "string" }
    },
    "dealBreakers": {
      "type": "array",
      "items": { "type": "string" }
    }
  }
}
